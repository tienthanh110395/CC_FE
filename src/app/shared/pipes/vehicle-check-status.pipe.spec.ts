/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { VehicleCheckStatusPipe } from './vehicle-check-status.pipe';

describe('Pipe: VehicleCheckStatuse', () => {
  it('create an instance', () => {
    let pipe = new VehicleCheckStatusPipe();
    expect(pipe).toBeTruthy();
  });
});
